<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.study.gst.cse_gr_app">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SubjectActivity"></activity>
        <activity android:name=".FaqActivity" />
        <activity android:name=".QActivity" />
        <activity android:name=".QaActivity" />
        <activity android:name=".AllTrackGr" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".GrActivity" />
        <activity android:name=".NongrActivity" />
        <activity android:name=".Intro.SignupActivity" />
        <activity android:name=".Intro.LoginActivity" />
        <activity
            android:name=".Intro.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>